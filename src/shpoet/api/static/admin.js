function field(id){return document.getElementById(id)}
async function loadConfig(){const c=await (await fetch('/admin/config')).json();Object.entries(c).forEach(([k,v])=>{const el=field(k);if(!el)return;if(el.type==='checkbox')el.checked=v;else el.value=v});field('adminStatus').textContent='Configuration loaded.'}
async function saveConfig(){const payload={model:field('model').value,temperature:Number(field('temperature').value),beam_width:Number(field('beam_width').value),max_length:Number(field('max_length').value),checkpoint_interval:Number(field('checkpoint_interval').value),use_critic:field('use_critic').checked,use_chooser:field('use_chooser').checked,anchor_pressure:Number(field('anchor_pressure').value)};const response=await fetch('/admin/config',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});field('adminStatus').textContent=response.ok?'Configuration saved.':await response.text()}
field('saveConfig').onclick=saveConfig;loadConfig().catch(e=>field('adminStatus').textContent=e.message);
